===== 符号约定 (重要!) =====

【深度数据 depth_buffer / CBuffer】
- bid_qty_[i]: 买盘第i+1档数量，存储为 **正数**
- ask_qty_[i]: 卖盘第i+1档数量，存储为 **负数** (卖方用负值表示)
- bid_amt_[i], ask_amt_[i]: 同上规则 (卖方金额为负)
- 使用时注意: 计算失衡等需要两边都为正时，ask 侧要取反
  例: float v_ask = -ask_qty_[0].back();  // 负→正

【订单流 TickData.LOB_Feature】
- volume: **正数** (uint32_t)，不带方向
- 方向由 order_dir 决定: L2::OrderDirection::BID / ASK
- price: 分 (0.01元单位)，转换为元需 * 0.01
- 金额计算: amt = volume * price / 10000.0f  // 万元


降频算子迁移流程（L0 Tick → L1 Minute）：
1. FeaturesDefine.hpp — 移动特征定义
从 LEVEL_0_FIELDS 删除对应 X(...) 行
添加到 LEVEL_1_FIELDS（放在 cs_spread_rank 之前，即 TS 区末尾）
改三处：
valid_type: DEPTH → DATA
PSD: "100/00/00" → "00/100/00"
description: 末尾加 (降频)
2. ComputeGraph.hpp — 移动 CBuffer + 算子声明
从 DAG::L0 struct 删除 CBuffer 和算子实例
添加到 DAG::L1 struct（构造函数之前）
输入引用加 l0. 前缀：BidQty_ → l0.BidQty_
CBuffer 模板用 ::L2::BLEN（L1 struct 内需要 :: 前缀避免歧义）
若算子有 Layer 依赖（如 ParaImba 依赖 Para 输出），两层一起搬，内部引用不变
3. Tick_Sequential.hpp — 删除 L0 调度
删除 Trigger_onDepth 块内对应的 compute() / flush() 调用
删除 ts_features_buffer_[L0_FieldOffset::xxx] = ... 写入行
TS_WRITE_FEATURES 范围不用改（用的是 enum 名，自动重编号）
4. Minute_Sequential.hpp — 添加 L1 调度
在 Trigger_onMinute 块内添加 compute() / flush()，注意 Layer 顺序（先 L1 再 L2）
添加 ts_features_buffer_[L1_FieldOffset::xxx] = dag_.l1.Xxx_.back() 写入行
TS_WRITE_FEATURES 末端从 tar_5 扩展到新的最后一个 TS 特征（如 depth_repre）
注意事项：
算子 class 头文件按照FeaturesDefine类型分类
若算子有 reset()，需要检查 DAG::reset_at_day_start() 是否需要同步调整
宏系统自动处理 offset/width/enum 重编号，不需要手动改 FeatureStoreConfig.hpp已经列完了，没有中断。
上面就是完整的降频迁移四步流程。完整内容已经全部输出，没有遗漏。四步流程加注意事项就是全部内容。
