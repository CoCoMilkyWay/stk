1. 实现有不合理的地方吗(没有得到真正有用的特征)? 同时参考FeaturesDefine.hpp公式定义
尽量复用相同trigger采样域的Cbuffer, 避免重复计算
2. 有优化空间吗, 需要高性能, 会run上亿次
注意, 为了统一和通用, compute和flush需要保持分离
3. 数据会跨天, 可以保留reset接口(减少计算量), 系统保证跨天时调用, 然后再compute和flush特征(如果需要, 没需要就不要声明)
如果声明了reset, 那么要保证在DAG的reset_for_new_day里直接调用即可, 不应加额外逻辑
4. 保留注释
5. 如果改了Cbuffer关系(DAG), 需要同步改ComputeGraph.hpp和Tick_Sequential.hpp的注释
