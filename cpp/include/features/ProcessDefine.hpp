/*

sec level =====================================================================

min level =====================================================================
abs(X)
sign(X)
sqrt(X)
exp(X)
log1p(X)

add(X, Y)
sub(X, Y)
mul(X, Y)
div(X, Y)

ts_mean(X, window)
ts_mean_decay(X, window)
ts_median(X, window)
ts_quantile(X, window, q)
ts_std(X, window)
ts_skew(X, window)
ts_kurt(X, window)
ts_diff(X, window)
ts_delay(X, window)
ts_max(X, window)
ts_min(X, window)
ts_argmax(X, window)
ts_argmin(X, window)
ts_rank(X, window)
ts_norm(X, window)

ts_corr_spearman(X, Y, window)
ts_corr_pearson(X, Y, window) # mainly for ts
ts_cov(X, Y, window)
ts_ols_resid(X, Y, window) # 共线/协整残差
ts_proj_resid(X, Y, window) # 投影残差

cs_norm(X, window)
cs_rank(X, window)

cs_corr_spearman(X, Y, window) # mainly for cs
cs_corr_pearson(X, Y, window)

https://tsfresh.readthedocs.io/en/latest/text/list_of_features.html
*/
